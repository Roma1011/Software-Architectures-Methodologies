## პრობლემა

მონაცემებს ასინქრონულად იღებთ და შედეგების **მიბმა (data binding)** გჭირდებათ (მაგალითად, Model-View-ViewModel დიზაინის ViewModel-ში).

## გადაწყვეტა

როცა თვისება მონაცემთა მიბმაში გამოიყენება, მან **დაუყოვნებლივ და სინქრონულად** უნდა დააბრუნოს რაიმე შედეგი. თუ რეალური მნიშვნელობა ასინქრონულად უნდა განისაზღვროს, შეგიძლიათ **ნაგულისხმევი** შედეგი დააბრუნოთ და მოგვიანებით თვისება სწორი მნიშვნელობით განაახლოთ.

გახსოვდეთ, რომ ასინქრონული ოპერაციები შეიძლება **ჩავარდნენ**, ისევე როგორც წარმატებით დასრულდნენ. რაკი ViewModel-ს წერთ, მონაცემთა მიბმით შეგიძლიათ UI შეცდომის მდგომარეობისთვისაც განაახლოთ.

`Nito.Mvvm.Async` ბიბლიოთეკას აქვს ტიპი **`NotifyTask`**:

```csharp
class MyViewModel
{
    public MyViewModel()
    {
        MyValue = NotifyTask.Create(CalculateMyValueAsync());
    }

    public NotifyTask<int> MyValue { get; private set; }

    private async Task<int> CalculateMyValueAsync()
    {
        await Task.Delay(TimeSpan.FromSeconds(10));
        return 13;
    }
}
```

`NotifyTask<T>` თვისების სხვადასხვა ქვეთვისებაზე მიბმა შესაძლებელია:

```xml
<Grid>
   <Label Content="იტვირთება..."
         Visibility="{Binding MyValue.IsNotCompleted,
                Converter={StaticResource BooleanToVisibilityConverter}}"/>
   <Label Content="{Binding MyValue.Result}"
         Visibility="{Binding MyValue.IsSuccessfullyCompleted,
                Converter={StaticResource BooleanToVisibilityConverter}}"/>
   <Label Content="მოხდა შეცდომა" Foreground="Red"
         Visibility="{Binding MyValue.IsFaulted,
                Converter={StaticResource BooleanToVisibilityConverter}}"/>
</Grid>
```

MvvmCross ბიბლიოთეკას აქვს `MvxNotifyTask`, რომელიც `NotifyTask<T>`-ის თითქმის იგივეა.

<svg viewBox="0 0 780 230" xmlns="http://www.w3.org/2000/svg" style="max-width:100%; height:auto; font-family: sans-serif;">
  <text x="390" y="22" text-anchor="middle" font-size="12" font-weight="bold" fill="currentColor">ერთი Task, სამი UI მდგომარეობა</text>

  <rect x="290" y="45" width="200" height="40" fill="#4A90D9" fill-opacity="0.2" stroke="currentColor" rx="5"/>
  <text x="390" y="70" text-anchor="middle" font-size="10" font-weight="bold" fill="currentColor">NotifyTask&lt;int&gt;</text>

  <line x1="340" y1="85" x2="160" y2="120" stroke="currentColor"/>
  <line x1="390" y1="85" x2="390" y2="120" stroke="currentColor"/>
  <line x1="440" y1="85" x2="620" y2="120" stroke="currentColor"/>

  <rect x="40" y="122" width="230" height="80" fill="#E67E22" fill-opacity="0.16" stroke="currentColor" rx="5"/>
  <text x="155" y="145" text-anchor="middle" font-size="10" font-weight="bold" fill="currentColor">IsNotCompleted</text>
  <text x="155" y="168" text-anchor="middle" font-size="9" fill="currentColor">„იტვირთება..." spinner</text>
  <text x="155" y="188" text-anchor="middle" font-size="9" fill="currentColor">საწყისი მდგომარეობა</text>

  <rect x="285" y="122" width="210" height="80" fill="#27AE60" fill-opacity="0.16" stroke="currentColor" rx="5"/>
  <text x="390" y="145" text-anchor="middle" font-size="10" font-weight="bold" fill="currentColor">IsSuccessfullyCompleted</text>
  <text x="390" y="168" text-anchor="middle" font-size="9" fill="currentColor">Result ჩანს</text>

  <rect x="510" y="122" width="230" height="80" fill="#C0392B" fill-opacity="0.16" stroke="currentColor" rx="5"/>
  <text x="625" y="145" text-anchor="middle" font-size="10" font-weight="bold" fill="currentColor">IsFaulted</text>
  <text x="625" y="168" text-anchor="middle" font-size="9" fill="currentColor">შეცდომის შეტყობინება</text>
  <text x="625" y="188" text-anchor="middle" font-size="9" fill="currentColor">ErrorMessage</text>
</svg>

*ნახაზი 14.3: მიბმადი მდგომარეობები ერთი task-იდან.*

## განხილვა

შესაძლებელია საკუთარი მიბმის გარსის დაწერაც, ბიბლიოთეკების ნაცვლად. ქვემოთ ძირითადი იდეაა:

```csharp
class BindableTask<T> : INotifyPropertyChanged
{
    private readonly Task<T> _task;

    public BindableTask(Task<T> task)
    {
        _task = task;
        var _ = WatchTaskAsync();
    }

    private async Task WatchTaskAsync()
    {
        try
        {
            await _task;
        }
        catch
        {
        }

        OnPropertyChanged("IsNotCompleted");
        OnPropertyChanged("IsSuccessfullyCompleted");
        OnPropertyChanged("IsFaulted");
        OnPropertyChanged("Result");
    }

    public bool IsNotCompleted { get { return !_task.IsCompleted; } }

    public bool IsSuccessfullyCompleted
    {
        get { return _task.Status == TaskStatus.RanToCompletion; }
    }

    public bool IsFaulted { get { return _task.IsFaulted; } }

    public T Result
    {
        get { return IsSuccessfullyCompleted ? _task.Result : default; }
    }

    public event PropertyChangedEventHandler PropertyChanged;

    protected virtual void OnPropertyChanged(string propertyName)
    {
        PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(propertyName));
    }
}
```

> [!IMPORTANT] ორი „უცნაურობა" აქ განზრახაა
> 1. **ცარიელი `catch` მუხლი**: ეს კოდი სპეციალურად **ყველა** გამონაკლისის დაჭერას ისახავს მიზნად, რომ ეს მდგომარეობები მონაცემთა მიბმით დაამუშაოს.
> 2. **`ConfigureAwait(false)` განზრახ არ არის**: `PropertyChanged` მოვლენა **UI ნაკადზე** უნდა აღიძრას ([[თავი 2.7 კონტექსტის არიდება გაგრძელებებისთვის|2.7]]).

> [!TIP] პაკეტები
> `NotifyTask` ტიპი `Nito.Mvvm.Async` NuGet პაკეტშია, `MvxNotifyTask` კი `MvvmCross`-ში.

> [!NOTE] `var _ = WatchTaskAsync();`
> ეს ჩანაწერი განზრახ არის: კონსტრუქტორი `async` ვერ იქნება, ამიტომ task **მიზანმიმართულად იგნორირდება**. ეს უსაფრთხოა მხოლოდ იმიტომ, რომ `WatchTaskAsync` თავის შიგნით **ყველა გამონაკლისს იჭერს** ([[თავი 11.2 ასინქრონული კონსტრუირება ფაბრიკებით|11.2]]-ის „ცუდი კოდისგან" განსხვავებით).

## იხილეთ ასევე

- [[თავი 1.2 ასინქრონული პროგრამირების შესავალი|1.2]]: `async`/`await`-ის საფუძვლები.
- [[თავი 2.7 კონტექსტის არიდება გაგრძელებებისთვის|2.7]]: `ConfigureAwait`-ის გამოყენება.
- [[თავი 11.4 ასინქრონული თვისებები|11.4]]: „ასინქრონული თვისებები" მიბმის გარეშე.

---

წინა [[თავი 14.2 System.Reactive-ის გადავადებული შეფასება]]
შემდეგი [[თავი 14.4 იმპლიციტური მდგომარეობა]]
